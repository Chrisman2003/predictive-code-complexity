This Directory will house either full datasets to train the predictive models on OR 
The Webpaths to access these datasets

The following Hierarchy is used to organize the loaded Data Components:
1. raw/:        Cloned HPC benchmark repositories
2. metadata/:   Natural language task descriptions & paradigm mappings
3. processed/:  Clean Parquet/JSON dataset ready for training
4. splits/:     Split data into training, validation, test